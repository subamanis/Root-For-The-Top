using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObstacleRandomGeneration : MonoBehaviour
{
    public Transform target;
    // Start is called before the first frame update
    void Start()
    {
        //var randomPosition = Random.insideUnitCircle * 4;
        //Debug.Log(new Vector2(target.position.x - randomPosition.x, target.position.y - randomPosition.y));
        //Debug.Log(target.position);

        //transform.position = new Vector2(target.position.x - randomPosition.x, target.position.y - randomPosition.y);
        //Debug.Log(randomPosition);
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
